name: post

on:
  # schedule:
  #   - cron: "30 14 * * *"
  workflow_dispatch:
jobs:
  build:
    name: post
    runs-on: ubuntu-latest
    steps:
      # - name: ls
      #   run: ls -l
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: "18"
          cache: npm
      - name: Install Dependencies
        run: npm ci
      - name: Create Sitemap
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{secrets.SUPABASE_SERVICE_ROLE_KEY}}
        run: npm run post $SUPABASE_URL SUPABASE_SERVICE_ROLE_KEY